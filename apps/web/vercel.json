{
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts",
  "buildCommand": "mkdir -p node_modules/@prisma && (cp -a ../../node_modules/@prisma/client node_modules/@prisma/client 2>/dev/null || true) && PRISMA_GENERATE_SKIP_AUTOINSTALL=1 npx prisma generate && next build",
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/automations",
      "schedule": "0 6 * * *"
    }
  ]
}
