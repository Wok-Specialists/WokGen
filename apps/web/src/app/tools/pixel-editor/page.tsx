'use client';
import ToolShell from '@/components/tools/ToolShell';

export default function Page() {
  return (
    <ToolShell
      id="pixel-editor"
      label="Pixel Art Editor"
      description="Browser-based pixel art editor with grid, pencil, fill, and eraser."
      icon="🖍️"
    />
  );
}
